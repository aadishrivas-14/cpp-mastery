#pragma once

#include <string>
#include <vector>
#include <map>

/**
 * TODO: Implement a Simple ORM (Object-Relational Mapping)
 * 
 * Requirements:
 * - Model base class
 * - Repository pattern for CRUD operations
 * - Simple query builder
 * 
 * Features:
 * - save(model) - Insert/Update
 * - find(id) - Retrieve by ID
 * - findAll() - Retrieve all
 * - remove(id) - Delete
 * 
 * Implementation hints:
 * - Use in-memory storage (std::map) for simplicity
 * - Or use SQLite for real database
 */

// TODO: Implement Model base class and Repository
