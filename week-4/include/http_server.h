#pragma once

#include <string>
#include <functional>

/**
 * TODO: Implement a Simple HTTP Server
 * 
 * Requirements:
 * - Listen on a port
 * - Handle GET/POST requests
 * - Route requests to handlers
 * - Send HTTP responses
 * 
 * Implementation hints:
 * - Use POSIX sockets (socket, bind, listen, accept)
 * - Parse HTTP request headers
 * - Build HTTP response with status code and body
 * - Handle multiple connections (optional: use thread pool)
 * 
 * Simplified approach:
 * - Single-threaded server
 * - Support only GET requests
 * - Return simple text responses
 */

// TODO: Implement HTTPServer class
